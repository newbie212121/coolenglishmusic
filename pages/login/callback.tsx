// pages/login/callback.tsx
"use client";

// This page's content will be hidden by AuthGate until the auth state
// is resolved, after which AuthGate will redirect the user away.
export default function LoginCallback() {
  return null;
}